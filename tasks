1. Prof Kael requires 3 items from the user: anemometer, control board (racoon), power cables
2. The note in the shed should hint to who stole the control board (racoon in tents)
3. Shed key should be picked up manually by the user.
4. "You found a raccoon! It seems to be playing with a control board. It looks hungry — maybe try feeding it a snack? (use snack)"
5. The racoon should drop the control board after being fed. (take control board)
6. "You fed the raccoon. It dropped the control board! (take control board)"
7. The user should take a note from the garden with the code for the metal box. The code will be shown in the console for the user to see.
8. The user should open the metal box using the code from the note and take the anemometer from inside. (use code, take anemometer)
9. Use code should trigger a prompt for the user to enter the code. Use code will make the code dissapear from the inventory after being used.

1. When the user comes from the solar fields this message should be display "After walking for hours you find yourself in a desolate land. The desert stretches before you. Towers of sand cover the solar field. Heat shimmers across the horizon. You find a small hub that looks like it could have life(west)", from other directions there should be a different neutral message.
2. The user should be able to go to solar fields and navigate back to the desert hub.
3. The user should be asked what does he want to use in the solar fields to fix the solar panels. The user can either use 2 robotic parts (preffered - maximum efficiency) or water hose (less efficiency).
4. The items get removed from the inventory after being used. A message should be displayed after using the items that everything is fixed in the Solar Desert zone and the user can go back to other zones.